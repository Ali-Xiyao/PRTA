# 后续实验路线图（Phase20 Slim-S1 最终主线版）

> 当前最高优先级：执行
> [Slim-S1 最终主线与重跑矩阵](13_PRTA-CXR-Slim-S1最终主线与重跑矩阵_CN.md)。
> Phase16/V2 队列保留为历史，凡与最终 S1 有关的训练/推理均进入新 Phase20
> 命名空间；外部验证最后执行，医生人工、Internal-test 与 Gold 不做。

## 1. 冻结边界与论文主张

更新时间：2026-08-18。

- V2 的 33-cell 核心矩阵、PRIOR diagnostics、Bootstrap 与 Phase15 可信性证据
  继续冻结为历史，不覆盖；最终投稿主方法改为 Slim-S1，相关证据按 Phase20
  新配置重跑/重推理。
- F02 的平均 Macro-F1 略高于 V2，但差异区间跨 0；V2 的主要优势改写为
  **competitive Macro-F1 + 更低 ODER**，不再声称全面性能冠军。
- A01/A03 是“finding-conditioned、relation-dependent”主结构证据；A02 作为中等
  效应报告；其他训练目标消融以 effect size、CI 和 Seed consistency 为主，不声称
  每项均显著。
- 错误、缺失和反转 PRIOR 会造成明显退化，因此它们证明的是历史依赖和部署脆弱性，
  不是鲁棒性。
- 外部/跨源独立数据仍因数据合同、标签映射和严格去重而最后执行；医生人工、reader
  study 和人工伤害类型标注不做。
- V2 与全部既有结果作为开发证据继续不可变保留；Slim-S1 已从 Train-only
  选择面锁定，Phase20 不再选择模型，只确认并补齐最终证据。

## 2. PRTA-CXR-Slim 最小精简矩阵（已正式完成）

结构核心固定保留 finding conditioning、cross-time attention、temporal relation
residual、ODC 和 matched-hard CMCP；DMW 固定关闭。只改变 standalone prototype CE
与 state anchor，形成冻结 2×2：

| Arm | DMW | Prototype CE | State anchor | ODC | matched-hard CMCP |
| --- | ---: | ---: | ---: | ---: | ---: |
| Slim-S0 | — | ✓ | ✓ | ✓ | ✓ |
| Slim-S1 | — | — | ✓ | ✓ | ✓ |
| Slim-S2 | — | ✓ | — | ✓ | ✓ |
| Slim-S3 | — | — | — | ✓ | ✓ |

每个 arm 固定 Seeds 17/28/43，共 12 个训练单元。历史 `w/o DMW` 只作描述性参照，
不能替代新选择面上的 Slim-S0。选择面只来自原 Train 患者：确定性五折中的 fold0
作为 20% Slim-Dev，其余四折作为 Inner-Train；原 Dev、Internal-test、Gold 与外部
结果均不得参与选择或调试。

运行前冻结规则：Macro-F1 距四 arm 最佳不超过 0.003，ODER 距最低不超过
0.0005，每类 recall 距该类最佳不超过 0.01；同时满足时按可选模块数量最少、再按
arm ID 选择。若没有共同 admissible arm，保留 Slim-S0 作为“不进一步精简”结论，
禁止临时改容差、补 arm、换 Seed 或调权重。

2026-08-18 正式结果：12 个唯一单元全部 PASS；`Slim-S0` 与 `Slim-S1` 通过
全部冻结门，`Slim-S2`/`Slim-S3` 未通过逐类别 recall 门，最终选择 `Slim-S1`
（删除 Prototype CE、保留 State anchor）。offload reconciliation、两条原始 lane
completion 与 finalizer 均 PASS。服务器重复/中断的 Seed43 副本已隔离，不进入
汇总。Phase16 两个 queue parent 继续保持 `T`，本次收口未自动恢复后续实验。

## 3. Phase16 P0 修订与重跑矩阵

| 证据轴 | 原问题 | 修订协议 | 是否重跑 |
| --- | --- | --- | --- |
| 内部纵向比较器 | 错称 official | 改为 BioViL-T-style Temporal Transformer、CheXRelNet-inspired Global Relation Network、TILA-style + Temporal Inversion；统一标注 `architecture_inspired_internal_reimplementation` | 数值可复用；finalizer 归一化历史身份 |
| source-held exploratory | 单源训练同时把 offline hard CMCP 改为 in-batch roll，存在混杂 | 明确命名 `source-held V2-roll exploratory` | 已跑单元不进入主 source-shift 结论；按加强后的 receipt/roster 协议重跑 |
| source-held confirmatory | 缺少同方法干净对照 | 新增 V1/no-CMCP：MIMIC-only 与 CHEX-only，各 3 Seeds；保留 prototype alignment | 新增 6 个训练 + 6 个 target-source Dev 评估 |
| PRIOR P5 | “older”不保证同 CURRENT 和真实时间顺序 | 只允许同 patient/finding/current study/current image，且 `candidate_prior_time < original_prior_time < current_time`；无合格样本时 fail closed | 修正版三 Seed推理重跑 |
| PRIOR P7 | 同时混合患者错配与 view 错配 | 正式命名 `wrong-patient view-mismatched PRIOR`，不作纯 view 因果解释 | 修正版三 Seed推理重跑 |
| PRIOR P8 | token 操作被误称 image corruption | 正式命名 `token-scrambled PRIOR` | 修正版三 Seed推理重跑 |
| finding F4 | 原置换由 finding 名决定，不是 sample-level | `hash(sample_id, salt)` 生成错误 finding，固定 17/28/43 三个 salts，报告均值与分层结果 | 修正版三 Seed推理重跑 |
| finding F5 | 同义替代同时改变 prompt 模板 | 保持 `chest x-ray finding: ...` 模板，改称 `clinical semantic alternative`；paraphrase 才允许完整句式变化 | 修正版三 Seed推理重跑 |
| corruption cache | 缺少完整输入身份收据 | 强制 Dev current-image 预检、split/condition/source/image roster SHA 与 cache manifest 一致 | blur/contrast/JPEG cache 重建 |
| scaling/noise/map | 科学设计基本正确，但审计字段不足 | 增加 patient/source/class/finding counts、实际噪声率、5×5 转移矩阵、changed-by-source/finding、transformed roster 与 map target/candidate coverage | 不因数值协议重跑已完成单元；后续生成物必须通过加强审计 |

修正版 modality、source-held-roll 和 V1 source-held 使用新的任务/输出命名空间，旧
PASS 即使仍在服务器也不会被 Phase16 finalizer 选入正式汇总。

## 4. Phase16 自动收口

新增统一 finalizer，禁止人工从多个 retry 目录抄数。它必须：

1. 同时读取 main、retry、repair/correction shared-state roots；
2. 以修正版 87-job registry 的 scientific job ID 为唯一主键；
3. 每个预期任务只能有一个 PASS，缺失 PASS 或重复 PASS 均 fail closed；
4. 校验 state、expected output、config、checkpoint、training receipt、input hashes、
   Seed、source commit 和 protected-read flags；
5. failed/skipped/旧协议尝试仅保留在 audit，不进入均值；
6. 自动生成三 Seed mean ± SD，明确
   `selection_performed=false`、`winner_selected=false`；
7. 只有 finalizer PASS 后，Phase16 数字才能进入论文表格。

## 5. 非外部实验的执行优先级

### P0：Slim 已完成；Phase16 保持暂停，待显式恢复

1. 修正版 modality assets 与三 Seed modality stress；
2. V2-roll exploratory source-held 重跑；
3. V1/no-CMCP confirmatory source-held 新增训练与评估；
4. state-pruning parity 与 pruned efficiency；
5. Phase16 finalizer 自动收口。

队列仍采用两张 A800 的 LPT 估时均衡。Slim 已 PASS，但按当前暂停边界，剩余
main/corrected 队列不会自动恢复；只有收到新的明确执行指令并重新完成进程、输入与
队列哈希预检后才可继续。

当前已冻结 38 个受影响任务，两卡标称负载分别为 76,560 s 与 76,500 s，仅差 60 s。
12 个尚未开始的旧 source-held 混杂协议任务已按 superseded 审计跳过，替代的
V2-roll exploratory 与 V1/no-CMCP confirmatory 单元完整保留。它们当前仅是待恢复
队列，不代表正在运行。

### P1：无需重新训练的可信融合证据

#### 4.1 PRIOR validity routing / fallback（冻结预测基础模拟已完成）

冻结 V2 和 Current-only，规则本身不学习新参数：

```text
patient identity mismatch  -> reject PRIOR
prior time >= current time -> reject PRIOR
missing PRIOR              -> current-only fallback 或 abstain
unsupported/cross view     -> warning / 预冻结策略
extreme sensitivity        -> cross-fitted threshold 下 abstain
```

固定比较：Always-V2、Invalid→Current-only、Invalid→abstain；分别在 true、null、
matched-hard wrong、reversed PRIOR 上报告 Macro-F1、ODER、coverage、error rate 和
fallback/abstention rate。阈值只能预冻结或患者级 cross-fitting，不能根据最终结果调参。

已完成的基础模拟把 synthetic intervention 身份视为已知无效标记，不拟合阈值：
Current-only fallback 在 matched-hard/null/reversed 上的 Macro-F1 均为 0.412872，
相比 Always-V2 的 0.342056/0.249746/0.191750 明显恢复；reversed ODER 从
0.171324 降至 0.035116。该结果只作为 oracle-detectable routing 上界；真实
patient/time/view validity checker 仍需独立冻结和验证。

#### 4.2 多目标 Pareto 与多重比较

- 绘制 Macro-F1–ODER–cost Pareto 图，至少包含 V2、F01、F02、Current-only；
- 同表报告 Balanced Accuracy、NLL、ECE、AURC、GFLOPs、batch-1 latency、trainable
  parameters；
- Holm families 预先划分为：主结构 A01/A02/A03、训练目标
  A04/A05/A06/A08/A10/A11、通用融合器 F01/F02；
- 同时提供把 11 项全部视为单一检验族的保守 sensitivity analysis；由于分族是在
  已有结果后固化，正文不把分族校正包装成预注册确认性检验；
- 同时保留未经调整的 effect size/95% CI 和 Holm-adjusted p，不按显著性删结果。

#### 4.3 模态压力分层

正式总表之外，按 progression class、finding、source、view relation、interval bin
分层；重点检查 New/Resolved 在 reversed PRIOR、Stable 在 null PRIOR、稀有 finding
的 prompt 敏感度和两数据源对 current corruption 的差异。

### P2：效率、grounding 与 supporting analyses

- state-pruning 必须先满足全部 predictions 一致且 `max_abs_logit_diff <= 1e-6`，再
  报告速度变化；
- 增加 raw-image endpoint latency：read/decode、preprocessing、BiomedCLIP early
  blocks、H2D、Tail8、fusion、classifier，以及 total p50/p95；缓存特征延迟继续明确
  标为 cached-representation efficiency；
- 完成 prototype geometry / grounding；
- 将 scaling curve、synthetic label-noise curve、亚组/长尾放入 supporting evidence；
  label noise 不外推为真实临床标注误差。

## 6. 真正官方方法与外部数据：最后阶段

当前三项是内部架构启发式重实现，不是 official baseline。真正官方比较单列：

1. 官方 BioViL-T；
2. 官方 CheXRelNet；
3. 官方 TILA。

只有在官方仓库/checkpoint/预处理可获得且统一五分类 head 的适配协议可审计时执行；
若官方代码缺失，可按论文复现，但必须标为 independent paper-based
reimplementation，不能再次使用 official 字样。

外部证据仍最后做：优先 CheXTemporal Gold，其次 Silver，再考虑 MS-CXR-T 或真正
独立机构数据。读取外部标签前必须冻结 checkpoint roster、finding/class mapping、
预处理、患者级严格去重、主要终点、bootstrap seed 和 excluded-row rules。

## 7. 论文材料的最终落盘顺序

1. Slim Train-only 2×2 结果、冻结选择回执与方法精简叙事；
2. Phase16 corrected final aggregate 与 provenance inventory；
3. 主消融/融合表：三 Seed mean ± SD、effect size、patient-cluster CI、Holm p；
4. PRIOR stress + safety routing 表；
5. F1–ODER–cost Pareto 图；
6. calibration/risk-coverage 与 state-pruning/endpoint efficiency；
7. source-held、scaling/noise、subgroup/long-tail/grounding 附表；
8. 最后加入 official/independent longitudinal comparison 与严格外部评估；
9. 全部收口后执行 `STOP_ALL_MODEL_AND_EXPERIMENT_SELECTION`。

Slim 双 lane/finalizer 现已 PASS。全局 STOP 仍要求两条修正版 A800 lane 与
Phase16 finalizer 也全部 PASS，且没有残留训练/评估进程；在此之前，Phase16 保持
暂停，不因 Slim 完成而自动续跑。最终 STOP 生成后不再自动启动任何训练、评估、
比较、选择或外部实验；Slurm 资源与无关 telemetry 不由本规则取消。

## 8. 明确不做与不应声称

- 不修改或删除冻结 V2/33-cell 历史证据；Slim 使用独立新命名空间，不覆盖旧结果；
- 不因 F02 单一均值更高而重新选主方法；
- 不把内部 style/inspired 比较器写成官方实现；
- 不把错误 PRIOR 下的崩溃写成鲁棒性；
- 不把缓存 token 延迟写成原始影像端到端延迟；
- 不把 synthetic label noise 写成真实放射科标注误差；
- 不开展医生人工或 reader study；
- 不在外部合同、标签映射和严格去重冻结前启动外部评估。
