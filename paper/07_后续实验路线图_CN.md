# PRTA-CXR 后续实验路线图（最终收口版）

更新时间：2026-08-22。

## 1. 总体状态

已授权的非外部 GPU 科学实验全部完成。主方法名称统一为 **PRTA-CXR**，`Slim-S1`
仅保留为配置追溯名。当前不再启动新的模型选择、训练、比较、外部验证或医生人工
任务。

| 阶段 | 总量 | PASS | RUNNING | PENDING | 状态 |
| --- | ---: | ---: | ---: | ---: | --- |
| Phase20-A | 88 | 88 | 0 | 0 | finalizer PASS |
| Comparator | 24 | 24 | 0 | 0 | finalizer PASS |
| Phase20-B1 | 6 | 6 | 0 | 0 | repaired formal root PASS |
| Phase20-B2 | 28 | 28 | 0 | 0 | statistics PASS |
| Evidence finalizer | 1 | 1 | 0 | 0 | no-selection PASS |

## 2. 已完成证据

- 主训练、结构/损失/融合消融、data scaling、label noise。
- 双向 MIMIC-CXR/CheXpert Plus source-held cross-source generalization。
- 8 方法 × 3 Seeds comparator。
- 校准、risk-coverage、选择性预测、三 Seed disagreement。
- descriptive subgroups/long-tail。
- state-pruning exact parity 与固定 A800 cached-feature efficiency。
- safety routing、exclusive correctness、10,000 次患者聚类 bootstrap、Holm 校正。
- Figure 5 native temporal attention flow 与 Figure S3 query specificity/stability；S3
  覆盖 1,743 个 multi-finding pairs，并通过预设 query-vs-seed JSD 门。

## 3. 明确不再运行

| 项目 | 决定 |
| --- | --- |
| ReXGradient/独立外部验证 | 退役；不报告旧结果，不重新启动 |
| Internal-test、Gold | 不打开 |
| 医生人工、reader study | 不做 |
| Phase C 可选压力扩展 | 不进入论文完成门，不自动补跑 |
| 新架构、新损失、重新选模 | STOP |

## 4. 仍需完成的论文工作（不占 GPU）

1. 将最终 comparator、B1、B2 表固定进论文主表和补充表。
2. 生成 calibration、risk-coverage、配对差异、Pareto 与效率图。
3. 把内部重实现统一标为 `architecture-inspired internal reimplementation` 或
   `independent paper-based reimplementation`，禁止写 official。
4. 统一核对正文、表格、图注中的 Seeds、CI、p 值与哈希来源。
5. 准备独立的 public-release 文件夹；只复制开源必需代码、配置、测试和说明，
   不复制内部 checkpoint、预测、数据、私有路径或运行日志。

## 5. 最终发布边界

PRTA GitHub 仓库只保存最终 PRTA-CXR 方法、必要复现实验代码、测试、Git-safe 聚合
结果和论文 Markdown。历史损失、失败路线、阶段快照和旧运行器进入 VisualVIT 历史
仓库。三个最终 best checkpoint 仅放在本地内部项目的 Git-ignored `checkpoints/final/`
下，并由 `manifests/final_prta_cxr_checkpoints.json` 固定哈希。
