# 后续实验路线图（Phase20 Slim-S1 最终主线版）

> 当前最高优先级：执行
> [Slim-S1 最终主线与重跑矩阵](13_PRTA-CXR-Slim-S1最终主线与重跑矩阵_CN.md)。
> Phase16/V2 的 Git-safe 历史固定在
> `codex/archive-v2-history-before-phase20`；旧私有 runtime/checkpoint 已清理。
> 凡与最终 S1 有关的训练/推理均进入新 Phase20 命名空间；正文泛化证据固定为
> 双向 MIMIC-CXR/CheXpert Plus source-held，独立外部验证、医生人工、
> Internal-test 与 Gold 不做。

## 1. 冻结边界与论文主张

更新时间：2026-08-20。

- V2 的 33-cell 核心矩阵、PRIOR diagnostics、Bootstrap 与 Phase15 可信性结论
  作为 Git-safe 历史汇总冻结；最终投稿主方法改为 Slim-S1，相关证据按 Phase20
  新配置重跑/重推理。旧 checkpoint、私有预测与运行副本不再保留。
- F02 的平均 Macro-F1 略高于 V2，但差异区间跨 0；V2 的主要优势改写为
  **competitive Macro-F1 + 更低 ODER**，不再声称全面性能冠军。
- A01/A03 是“finding-conditioned、relation-dependent”主结构证据；A02 作为中等
  效应报告；其他训练目标消融以 effect size、CI 和 Seed consistency 为主，不声称
  每项均显著。
- 错误、缺失和反转 PRIOR 会造成明显退化，因此它们证明的是历史依赖和部署脆弱性，
  不是鲁棒性。
- source-held 跨来源评估是正文主要泛化实验，必须完成两个方向的三 Seed、逐类指标、
  ODER、患者聚类 CI 与 in-domain/cross-source gap；它不能写成独立外部临床验证。
- ReXGradient 和其他独立外部验证已经退役，不再运行、不进入论文结果或模型选择；
  医生人工、reader study 和人工伤害类型标注同样不做。
- V2 与既有聚合结论作为开发证据在归档分支不可变保留；Slim-S1 已从 Train-only
  选择面锁定，Phase20 不再选择模型，只确认并补齐最终证据。

## 2. PRTA-CXR-Slim 最小精简矩阵（已正式完成）

结构核心固定保留 finding conditioning、cross-time attention、temporal relation
residual、ODC 和 matched-hard CMCP；DMW 固定关闭。只改变 standalone prototype CE
与 state anchor，形成冻结 2×2：

| Arm | DMW | Prototype CE | State anchor | ODC | matched-hard CMCP |
| --- | ---: | ---: | ---: | ---: | ---: |
| Slim-S0 | — | ✓ | ✓ | ✓ | ✓ |
| Slim-S1 | — | — | ✓ | ✓ | ✓ |
| Slim-S2 | — | ✓ | — | ✓ | ✓ |
| Slim-S3 | — | — | — | ✓ | ✓ |

每个 arm 固定 Seeds 17/28/43，共 12 个训练单元。历史 `w/o DMW` 只作描述性参照，
不能替代新选择面上的 Slim-S0。选择面只来自原 Train 患者：确定性五折中的 fold0
作为 20% Slim-Dev，其余四折作为 Inner-Train；原 Dev、Internal-test、Gold 与外部
结果均不得参与选择或调试。

运行前冻结规则：Macro-F1 距四 arm 最佳不超过 0.003，ODER 距最低不超过
0.0005，每类 recall 距该类最佳不超过 0.01；同时满足时按可选模块数量最少、再按
arm ID 选择。若没有共同 admissible arm，保留 Slim-S0 作为“不进一步精简”结论，
禁止临时改容差、补 arm、换 Seed 或调权重。

2026-08-18 正式结果：12 个唯一单元全部 PASS；`Slim-S0` 与 `Slim-S1` 通过
全部冻结门，`Slim-S2`/`Slim-S3` 未通过逐类别 recall 门，最终选择 `Slim-S1`
（删除 Prototype CE、保留 State anchor）。offload reconciliation、两条原始 lane
completion 与 finalizer 均 PASS。服务器重复/中断的 Seed43 副本不进入汇总，且其
旧私有运行副本已在 Phase20 启动后清理。原 Phase16 scientific step 已退休，两个
长期 allocation 现承载 Phase20，不再恢复旧队列。

## 3. Phase16 历史修订协议（已由 Phase20 取代）

| 证据轴 | 原问题 | 修订协议 | 是否重跑 |
| --- | --- | --- | --- |
| 内部纵向比较器 | 错称 official | 改为 BioViL-T-style Temporal Transformer、CheXRelNet-inspired Global Relation Network、TILA-style + Temporal Inversion；统一标注 `architecture_inspired_internal_reimplementation` | 数值可复用；finalizer 归一化历史身份 |
| source-held exploratory | 单源训练同时把 offline hard CMCP 改为 in-batch roll，存在混杂 | 明确命名 `source-held V2-roll exploratory` | 已跑单元不进入主 source-shift 结论；按加强后的 receipt/roster 协议重跑 |
| source-held confirmatory | 缺少同方法干净对照 | 新增 V1/no-CMCP：MIMIC-only 与 CHEX-only，各 3 Seeds；保留 prototype alignment | 新增 6 个训练 + 6 个 target-source Dev 评估 |
| PRIOR P5 | “older”不保证同 CURRENT 和真实时间顺序 | 只允许同 patient/finding/current study/current image，且 `candidate_prior_time < original_prior_time < current_time`；无合格样本时 fail closed | 修正版三 Seed推理重跑 |
| PRIOR P7 | 同时混合患者错配与 view 错配 | 正式命名 `wrong-patient view-mismatched PRIOR`，不作纯 view 因果解释 | 修正版三 Seed推理重跑 |
| PRIOR P8 | token 操作被误称 image corruption | 正式命名 `token-scrambled PRIOR` | 修正版三 Seed推理重跑 |
| finding F4 | 原置换由 finding 名决定，不是 sample-level | `hash(sample_id, salt)` 生成错误 finding，固定 17/28/43 三个 salts，报告均值与分层结果 | 修正版三 Seed推理重跑 |
| finding F5 | 同义替代同时改变 prompt 模板 | 保持 `chest x-ray finding: ...` 模板，改称 `clinical semantic alternative`；paraphrase 才允许完整句式变化 | 修正版三 Seed推理重跑 |
| corruption cache | 缺少完整输入身份收据 | 强制 Dev current-image 预检、split/condition/source/image roster SHA 与 cache manifest 一致 | blur/contrast/JPEG cache 重建 |
| scaling/noise/map | 科学设计基本正确，但审计字段不足 | 增加 patient/source/class/finding counts、实际噪声率、5×5 转移矩阵、changed-by-source/finding、transformed roster 与 map target/candidate coverage | 不因数值协议重跑已完成单元；后续生成物必须通过加强审计 |

修正版 modality、source-held-roll 和 V1 source-held 使用新的任务/输出命名空间，旧
PASS 即使仍在服务器也不会被 Phase16 finalizer 选入正式汇总。

## 4. Phase16 历史自动收口要求（不再恢复旧 runtime）

新增统一 finalizer，禁止人工从多个 retry 目录抄数。它必须：

1. 同时读取 main、retry、repair/correction shared-state roots；
2. 以修正版 87-job registry 的 scientific job ID 为唯一主键；
3. 每个预期任务只能有一个 PASS，缺失 PASS 或重复 PASS 均 fail closed；
4. 校验 state、expected output、config、checkpoint、training receipt、input hashes、
   Seed、source commit 和 protected-read flags；
5. failed/skipped/旧协议尝试仅保留在 audit，不进入均值；
6. 自动生成三 Seed mean ± SD，明确
   `selection_performed=false`、`winner_selected=false`；
7. 只有 finalizer PASS 后，Phase16 数字才能进入论文表格。

## 5. 非外部实验的执行优先级

> 2026-08-22 运维补充：已启用唯一线程 heartbeat `prta-cxr-phase20-b1-b2`，按
> comparator finalizer → B1 6/6 → B2 28/28 → evidence finalizer 的顺序自动接力。
> 两条本地 comparator lane 已各 5/5 PASS；服务器侧状态仍以串行 SSH 刷新结果为准。
> 服务器完成产物将在下游依赖闭合后按
> `docs/operations/Phase20服务器产物归档与清理协议_CN.md` 迁移和校验，checkpoint
> 与患者级产物不进入普通 Git。

### P0：Phase20 Slim-S1 正式确认队列（正在运行）

> **2026-08-21 更新**：Phase20-A 已正式完成。server/local host shard 分别
> 核验 60/60 与 28/28，全局 finalizer 为
> `PASS_PHASE20_A_FINAL_NO_SELECTION_AGGREGATE`，合计 63 training + 19 map +
> 6 source-held evaluation，并生成 21 组训练和 2 组跨来源三 Seed汇总。
> 全局凭据 SHA-256 为
> `a572f466907b5d5b9a3d151f5182d3652b22e83a8d44be1d8e0ddce23ac23b0d`。
> 当前 GPU 工作只剩既定 comparator 队列；Phase20-A 不再重跑。

1. full-Train S1 三 Seed；
2. 精确 loss/structure 消融与 F01/F02-DMW0；
3. S1-core/no-CMCP source-held、data scaling 与 label noise；
4. 63-cell/88-job Phase20-A 权威 finalizer；
5. 24-cell comparator rebuild 与 comparator finalizer；
6. Phase20-B1 的 6 个 focused trustworthiness jobs；
7. Phase20-B2 的 27 个比较器概率导出 + 1 个配对统计 job；
8. B1+B2 evidence finalizer 与论文级统计汇总。

旧私有 checkpoint 已按用户要求删除，因此旧计划中的“无需训练直接继承比较器”
只对 Git-safe 聚合叙事成立，不再满足患者级 paired bootstrap、safety routing 和
同协议最终表。Phase20-A 之后增加 24-cell 对比重建：V2、S0、B401、B402、
TILA8、BioViL-T-style、CheXRelNet-inspired、`TILAPaper` 各三 Seed。
内部/论文复现必须在表格中明确标注，不写成 official implementation。

正式 program 共 88 个依赖 job，其中 63 个为训练单元。队列采用服务器双 A800 +
本地 RTX3090 GPU0 的 LPT 估时均衡，RTX3090 GPU1 在初始冻结时保留。三条首任务
`P20-FINAL-S1-S17/S28/S43` 均已进入 RUNNING；A800 step 为
`3066.188`/`9929.148`。旧 Phase16 不会自动恢复。

三条 lane 的冻结 A800 等效负载为 333,470 s、326,770 s 与 327,167 s，最大差
6,700 s。source-held 的正式版本已经进入 Phase20 配置/依赖图，不再读取或恢复
旧 Phase16 runtime。

审查后的 post-training 修复不改变当前 63 个训练配置。2026-08-20 用户进一步授权
取消 comparator 的无必要全局空等：四条独立 watcher 改为只等待各自冻结的
Phase20-A PASS 门，随后立即启动本卡 comparator 队列；全局 88/88 finalizer 仍只用于
跨主机聚合、论文报告和下游 B2。修复版源码 `6100318` 与 comparator program 已在
本地/服务器哈希部署。新四卡 program 以独立命名空间冻结为 7/7/5/5，不得触碰当前
训练 child。旧全局 watcher 已退役，旧程序未启动且禁止再启动。
Slim-S1 B1 准备器强制三 Seed final checkpoint/receipt 齐全且 hash 一致；B2 还强制
Phase20-A/comparator 两个 finalizer PASS 和 27 组 checkpoint/receipt 哈希一致。

#### 5.1 初始 Phase20-A 与四卡续接控制快照

Phase20-A 的训练进度列保留 2026-08-18 15:44（Asia/Shanghai）的初始审计快照；
“后续对比接力”列已按 2026-08-20 生效的四卡 comparator 替换计划更新。
此表不是最终论文结果。`current_epoch=N` 表示第 N+1 个 epoch；
“单任务粗进度”按 `(已完成 epoch + 当前 epoch step 比例) / 20` 计算，只用于判断
进程是否持续前进。Best Dev Macro-F1 是中间 checkpoint 指标，不参与重新选方法。

| 资源 / lane | 当前任务 | 当前训练进度 | 中间 Best Dev Macro-F1 | Phase20-A 状态 | 后续对比接力 | 运行判断 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| 服务器 A800 / `a800_3066`（allocation 3066） | `P20-FINAL-S1-S17` | epoch 8/20，100/5,026 step；粗略 40.1% | 0.552486（epoch 4） | 1 RUNNING + 25 PENDING，共 26 job | 7 PENDING；per-lane watcher PID 3406948 `WATCHING` | 本卡 Phase20-A completion PASS 后立即接力 comparator |
| 服务器 A800 / `a800_9929`（allocation 9929） | `P20-FINAL-S1-S28` | epoch 7/20 完成；粗略 40.0% | 0.552794（epoch 4） | 1 RUNNING + 33 PENDING，共 34 job | 7 PENDING；per-lane watcher PID 3406949 `WATCHING` | 本卡 Phase20-A completion PASS 后立即接力 comparator |
| 本地 RTX3090 GPU0 / `rtx3090_0` | `P20-FINAL-S1-S43`，训练 PID 8132 | epoch 5/20 完成；粗略 30.0% | 0.550761（epoch 3） | 1 RUNNING + 27 PENDING，共 28 job | 5 PENDING；per-lane watcher PID 26844 `WATCHING` | 本卡 25 个唯一 Phase20-A state PASS 后立即接力 comparator |
| 本地 RTX3090 GPU1 / `rtx3090_1` | Phase20-A 尾部 handoff | 运行中 | 不用于选模 | 独立尾部队列 | 5 PENDING；per-lane watcher PID 13792 `WATCHING` | handoff completion PASS 后立即接力 comparator |

本地 S43 当前训练未被停止或迁移。原控制器引用活动 Git 工作树，后续 job 可能发生
source commit 漂移，因此只退休了父控制器；训练子进程 PID 8132 继续运行。恢复
控制器 PID 2632 会等待 S43 terminal PASS，再把剩余 Phase20-A lane 无缝切到只读
`source_2e38983`；不会重跑 S43，也不会与下一任务重叠。

finalizer coordinator PID 16264 继续等待 server/local host shard，并在 88-job 终态后
合并全局 finalizer、发布最终聚合凭据。四个 per-lane watcher 只轮询本卡门，在本卡
PASS 前不创建 `srun`、不占 GPU；它们不等待全局最终报告门。

#### 5.2 总体阶段进度

| 阶段 | 总量 | PASS | RUNNING | PENDING / 未冻结 | 当前门控 |
| --- | ---: | ---: | ---: | ---: | --- |
| Phase20-A 主训练与依赖 | 88 jobs：63 训练 + 25 map/evaluation | 88 | 0 | 0 | 全局 finalizer PASS；不再重跑 |
| post-cleanup comparator rebuild | 24 训练：8 systems × 3 Seeds | 14 | 4 | 6 | 2026-08-21 Git-safe 快照；4/8 方法齐三 Seed，最终汇总仍待 24/24 finalizer |
| Phase20-B1 Slim-S1 evidence | 6 jobs：3 PRIOR + calibration + subgroup + S43 state/efficiency | 0 | 0 | 6 个已冻结并入自动接力 | A800-3066 的 7-cell comparator 精确 PASS 后自动启动；当前 watcher=`WATCHING` |
| Phase20-B2 post-comparator | 28 jobs：27 概率导出 + 1 统计 | 0 | 0 | 28 个已构建、尚未正式冻结 | A/comparator finalizer、B1 S1 概率与 27 个 checkpoint/receipt 齐全 |
| Phase20-C optional evidence | 11 个现有可选 job + 4 类未构建扩展 | 0 | 0 | 不冻结、不入队 | 只有明确后续决定才单独冻结；不阻塞 B1/B2/finalizer |
| 医生人工 / Internal-test / Gold | 0 | 0 | 0 | 明确不做 / 封存 | 不进入当前队列 |

Phase20-A 已完成；当前 comparator 快照为 14 PASS、4 RUNNING、6 PENDING，B1 六个任务已单独冻结并
排在 A800-3066 自身 comparator lane 后。B1 当前仅是 `WATCHING`，不能冒充 RUNNING
或已有结果；B2 仍只计为代码已构建、输入门未满足。

#### 5.3 Phase20-A 每条卡剩余队列

表内顺序就是 lane 内执行顺序的分组表达；同组中的具体 Seed/变体均来自不可变
queue JSON。map 与 source-held evaluation 是依赖 job，不应误算为新训练单元。

| lane | 当前 job 后依次待跑的内容 | 待跑数 |
| --- | --- | ---: |
| `a800_3066` | loss ablation S17：`NOCMCP/NOODC/NOSTATE`；`F02-DMW0` S17/S43；structure S17：`NOALIGN/NOFINDING/NORELATION`；source-held：CHEX S17/S28、MIMIC S28 及 3 个 evaluation；scaling：F75/F50/F10 S28、F25 S17；noise：plausible 05/10/20 S17、symmetric 05/10 S17、symmetric 10/20 S43 | 25 |
| `a800_9929` | server map 10 个：noise 6 + scaling 4；loss ablation S28 三项；`F02-DMW0` S28、`F01-DMW0` S17/S43；structure S43 三项；source-held CHEX/MIMIC S43 及 2 个 evaluation；scaling S43 四档；noise plausible 05/10/20 S28、symmetric 05/10 S28、symmetric 20 S17 | 33 |
| `rtx3090_0` | local map 9 个：本 lane 所需 noise 5 + scaling 4；loss ablation S43 三项；`F01-DMW0` S28；structure S28 三项；source-held MIMIC S17 及 evaluation；scaling F75/F50/F10 S17、F25 S28；noise plausible 05/10/20 S43、symmetric 05 S43、symmetric 20 S28 | 27 |
| `rtx3090_1` | 已授权尾部 handoff：symmetric-noise 与 source-held evaluation | 3 |

#### 5.4 Phase20-A 完成后的 24-cell 对比接力

| lane | 已冻结的执行顺序 | 数量 | 冻结估时 |
| --- | --- | ---: | ---: |
| `a800_3066` | S0-S17 → B401-S28 → V2-S28 → B402-S28 → BioViLT-S43 → TILAPaper-S17 → CheXRelNet-S28 | 7 | 93,000 s |
| `a800_9929` | S0-S28 → B401-S43 → V2-S43 → TILA8-S17 → B402-S43 → TILAPaper-S28 → CheXRelNet-S43 | 7 | 93,000 s |
| `rtx3090_0` | S0-S43 → B401-S17 → TILA8-S28 → BioViLT-S17 → TILAPaper-S43 | 5 | 85,705 s |
| `rtx3090_1` | V2-S17 → TILA8-S43 → B402-S17 → BioViLT-S28 → CheXRelNet-S17 | 5 | 82,655 s |

四条接力分别要求本卡 Phase20-A PASS 门：A800 读取自身 lane completion，GPU1 读取
独占 handoff completion，GPU0 对其 25 个未移交 job 的唯一 PASS state 做哈希/身份
核验。全局 finalizer 仍由 server/local host shard 合并，核验 88 个唯一 PASS、
63/19/6 家族计数、queue/source/output hash 和 protected/external flags，并且是
最终汇总和后续 B2 的唯一依据；前序出现失败时对应 lane 写 BLOCK 收据，不会跳过失败继续跑。
`TILAPaper`、BioViLT 与 CheXRelNet 项分别标为
paper-based 或 architecture-inspired internal reimplementation，不声称官方实现。

#### 5.5 三个 final-S1 checkpoint 齐全后待冻结的 Phase20-B 最小证据包

| 证据组 | job | 数量 |
| --- | --- | ---: |
| probability / PRIOR stress | S17、S28、S43 全概率与 true/matched/null/reversed PRIOR | 3 |
| calibration / selective prediction | 三 Seed联合校准、risk-coverage、referral | 1 |
| subgroup / long-tail | progression、finding、source、view、interval、long-tail | 1 |
| fixed-Seed state-pruning + efficiency | 固定 S43；pruned logits parity，full/pruned 参数量、FLOPs、batch-1/16 latency、吞吐与显存 | 1 |
| **Phase B 可执行合计** | 仅 official Dev；不含外部、Internal-test、Gold、医生人工 | **6** |
| finalizer | 与 B2 共同做唯一 PASS、哈希和证据族对账 | 1 个收口单元 |

因此按论文完成单元计为 **6 个 Phase B job + 1 个 finalizer = 7**。generic
corruption 和 prompt robustness 不再作为 Information Fusion 主张的完成门。

Phase C 单列为可跑可不跑，不自动接在 Phase B 后：

| Phase C 可选族 | 当前目录 | 处理规则 |
| --- | ---: | --- |
| blur / contrast / JPEG 资产与三 Seed modality stress | 7 jobs | 有明确补充材料需求时才另冻队列 |
| S17/S28 额外 state-pruning export/parity | 4 jobs | S43 已覆盖代码路径与固定硬件 profiling，默认不跑 |
| older/view/token scramble、typo/synonym/paraphrase/random salts | 4 类，尚未构建 | 不为表格对称补跑；需要时再设计协议 |

该 B1 program 已以 6-job 独立命名空间冻结，源码提交为 `81a5e82`，队列 SHA-256
为 `66144b5015f683d92beb5fcef73b3f83f81457e970538d5d12e39734d4c0c221`。
唯一 CPU watcher 挂在 A800-3066 的 7-cell comparator completion 后：只有 completion
及七个独立 state 全部精确 PASS、queue/source hash 一致、失败/跳过为空且受保护数据
读取为零时才执行 B1；否则写 BLOCK，不提前占用 GPU，也不影响其他三条 comparator lane。

#### 5.6 Phase20-B2 与三层 finalizer（代码已构建，输入未就绪）

| 层 | 固定检查/输出 | 当前状态 |
| --- | --- | --- |
| Phase20-A finalizer | server/local host shard 各自验证后全局合并；63 training + 19 transformed maps + 6 source-held eval；唯一 PASS、config/checkpoint/receipt/input/best epoch/Seed/lane/source/queue/protected hash | **PASS：88/88；21 组训练 + 2 组 source-held 三 Seed汇总已生成** |
| comparator finalizer | server/local host shard 各自验证后全局合并；24/24；每方法三 Seed mean ± SD、逐类、ODER、参数量、训练时长、provenance、`official=false` | 代码完成；等 comparator terminal |
| B2 | 9 systems × 3 Seeds true-PRIOR export；safety、disagreement、risk-coverage、exclusive correctness、10,000 patient-cluster bootstrap、Holm | 28 jobs 已构建；等待两上游 finalizer 与 B1 S1 概率 |
| evidence finalizer | 对账 Phase B 6 PASS + B2 28 PASS；明确 `phase_c_optional_included=false`，输出 `PASS_PHASE20_EVIDENCE_FINAL_NO_SELECTION_AGGREGATE` | 代码完成；等待上游 |

`NOFINDING` 的训练行为不变，论文统一写为 **w/o visual finding-query
conditioning**。原 F1 统一解释为 **zero text embedding**；另增
`F1b_zero_projected_query` 在 learned projection 后直接置零，二者不得混称。
效率表当前只代表 cached-feature endpoint，不能写成 raw-image end-to-end latency。

checkpoint 保留门：Final S1、S0、F02-DMW0、TILA8 与最强兼容 paper-based
comparator 的三 Seed checkpoint 在 B1/B2、跨来源评估、最终图表和潜在审稿补实验结束前
不得删除；清理前必须保存 config/checkpoint/training receipt/input/best epoch/source/
hardware 的哈希身份。

### P1：full S1 与必要重建 checkpoint 上的可信融合证据

#### 4.1 PRIOR validity routing / fallback（冻结预测基础模拟已完成）

冻结 V2 和 Current-only，规则本身不学习新参数：

```text
patient identity mismatch  -> reject PRIOR
prior time >= current time -> reject PRIOR
missing PRIOR              -> current-only fallback 或 abstain
unsupported/cross view     -> warning / 预冻结策略
extreme sensitivity        -> cross-fitted threshold 下 abstain
```

固定比较：Always-V2、Invalid→Current-only、Invalid→abstain；分别在 true、null、
matched-hard wrong、reversed PRIOR 上报告 Macro-F1、ODER、coverage、error rate 和
fallback/abstention rate。阈值只能预冻结或患者级 cross-fitting，不能根据最终结果调参。

已完成的基础模拟把 synthetic intervention 身份视为已知无效标记，不拟合阈值：
Current-only fallback 在 matched-hard/null/reversed 上的 Macro-F1 均为 0.412872，
相比 Always-V2 的 0.342056/0.249746/0.191750 明显恢复；reversed ODER 从
0.171324 降至 0.035116。该结果只作为 oracle-detectable routing 上界；真实
patient/time/view validity checker 仍需独立冻结和验证。

#### 4.2 多目标 Pareto 与多重比较

- 绘制 Macro-F1–ODER–cost Pareto 图，至少包含 V2、F01、F02、Current-only；
- 同表报告 Balanced Accuracy、NLL、ECE、AURC、GFLOPs、batch-1 latency、trainable
  parameters；
- Holm families 预先划分为：主结构 A01/A02/A03、训练目标
  A04/A05/A06/A08/A10/A11、通用融合器 F01/F02；
- 同时提供把 11 项全部视为单一检验族的保守 sensitivity analysis；由于分族是在
  已有结果后固化，正文不把分族校正包装成预注册确认性检验；
- 同时保留未经调整的 effect size/95% CI 和 Holm-adjusted p，不按显著性删结果。

#### 4.3 Phase C 可选模态压力分层

该部分不进入正式完成门。只有收到明确的 Phase C 启动决定时，才按 progression
class、finding、source、view relation、interval bin 分层，并探索稀有 finding 的
prompt 敏感度和两数据源对 current corruption 的差异；不得为了表格对称自动补跑。

### P2：效率、grounding 与 supporting analyses

- state-pruning 必须先满足全部 predictions 一致且 `max_abs_logit_diff <= 1e-6`，再
  报告速度变化；
- 增加 raw-image endpoint latency：read/decode、preprocessing、BiomedCLIP early
  blocks、H2D、Tail8、fusion、classifier，以及 total p50/p95；缓存特征延迟继续明确
  标为 cached-representation efficiency；
- 完成 prototype geometry / grounding；
- 将 scaling curve、synthetic label-noise curve、亚组/长尾放入 supporting evidence；
  label noise 不外推为真实临床标注误差。

## 6. 真正官方方法与跨来源泛化

当前三项是内部架构启发式重实现，不是 official baseline。真正官方比较单列：

1. 官方 BioViL-T；
2. 官方 CheXRelNet；
3. 官方 TILA。

只有在官方仓库/checkpoint/预处理可获得且统一五分类 head 的适配协议可审计时执行；
若官方代码缺失，可按论文复现，但必须标为 independent paper-based
reimplementation，不能再次使用 official 字样。

正文泛化证据只使用已经冻结在 Phase20-A 中的两个方向：

1. MIMIC-CXR-only S1-core/no-CMCP 训练，CheXpert Plus Dev 评估；
2. CheXpert Plus-only S1-core/no-CMCP 训练，MIMIC-CXR Dev 评估。

两方向都使用 Seeds 17/28/43。每个方向必须报告 Macro-F1、Balanced Accuracy、
ODER、五类 F1/Recall、患者聚类 bootstrap 95% CI，以及 in-domain 与 cross-source
的性能差。公平参考使用同样关闭 CMCP 的 full-source `P20-ABL-NOCMCP`，避免把
训练来源变化与 CMCP 开关混在一起。

ReXGradient 及其他独立外部验证已经从活跃实验路线退役：不再启动队列、不再扩展
比较器、不报告既有结果，也不据其重新选模、调阈值或修改主方法。历史实现与私有
密封产物只作为审计记录保留，不属于论文材料或完成门。

## 7. 论文材料的最终落盘顺序

1. Slim Train-only 2×2 结果、冻结选择回执与方法精简叙事；
2. Phase16 corrected final aggregate 与 provenance inventory；
3. 主消融/融合表：三 Seed mean ± SD、effect size、patient-cluster CI、Holm p；
4. PRIOR stress + safety routing 表；
5. F1–ODER–cost Pareto 图；
6. calibration/risk-coverage 与 state-pruning/endpoint efficiency；
7. 双向 source-held 跨来源主表，以及 scaling/noise、subgroup/long-tail/grounding 附表；
8. 最后加入 official/independent longitudinal comparison；
9. 全部收口后执行 `STOP_ALL_MODEL_AND_EXPERIMENT_SELECTION`。

Slim 选择矩阵已 PASS；当前全局 STOP 改由 Phase20 活动队列、全部非外部依赖与
Phase20 finalizer 共同门控。最终 STOP 生成后不再自动启动任何训练、评估、比较、
选择或外部实验；Slurm 资源与无关 telemetry 不由本规则取消。

## 8. 明确不做与不应声称

- 不修改归档分支中的 Git-safe V2/33-cell 聚合历史；私有旧 runtime/checkpoint 按
  用户授权删除，Slim 使用独立 Phase20 命名空间；
- 不因 F02 单一均值更高而重新选主方法；
- 不把内部 style/inspired 比较器写成官方实现；
- 不把错误 PRIOR 下的崩溃写成鲁棒性；
- 不把缓存 token 延迟写成原始影像端到端延迟；
- 不把 synthetic label noise 写成真实放射科标注误差；
- 不开展医生人工或 reader study；
- 不启动或报告 ReXGradient/其他独立外部验证；历史私有结果不进入论文、选模、
  阈值或主方法修改。跨来源结果只写作 domain generalization。
